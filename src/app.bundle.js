// Generated by npm run build from the maintainable modules in src/.
(() => {
"use strict";
const VOCABULARY = [
  { id: "p1-exercise-algorithm", term: "algorithm", definition: "A procedure, instructions, or formula for solving a problem or completing a task.", zh: "演算法", page: 1, source: "exercise" },
  { id: "p1-exercise-machine-learning", term: "machine learning", definition: "A core sub area of AI by which computer programs can “learn” and automatically modify its knowledge, procedures, and processes to improve performance and efficiency.", zh: "機器學習", page: 1, source: "exercise" },
  { id: "p1-exercise-automation", term: "automation", definition: "The technique of making a machine, a process, or a system operate automatically.", zh: "自動化", page: 1, source: "exercise" },
  { id: "p1-exercise-deep-learning", term: "deep learning", definition: "A subset of machine learning that uses complex algorithms to mimic the brain’s neural network to learn, with little or no human supervision.", zh: "深度學習", page: 1, source: "exercise" },
  { id: "p1-exercise-big-data", term: "big data", definition: "A collection of data sets so large and complex that it becomes difficult to process using most typical data management tools. This is the raw fuel of AI, as it provides the inputs for surfacing patterns and making predictions.", zh: "大數據", page: 1, source: "exercise" },
  { id: "p1-exercise-artificial-intelligence", term: "artificial intelligence", definition: "The simulation of human intelligence by machines. Or more simply, having machines “think like a human.”", zh: "人工智慧", page: 1, source: "exercise" },
  { id: "p1-exercise-data-mining", term: "data mining", definition: "The process of combing through large sets of information to discover patterns and extract useful information.", zh: "資料探勘", page: 1, source: "exercise" },
  { id: "p1-exercise-internet-of-things", term: "Internet of Things (IoT)", definition: "A network of billions of digitally connected devices that collect and exchange data. These devices can also be programmed to communicate with one another to better serve users.", zh: "物聯網", page: 1, source: "exercise" },
  { id: "p1-exercise-chatbot", term: "chatbot", definition: "A computer program that simulates human conversation.", zh: "聊天機器人", page: 1, source: "exercise" },
  { id: "p1-exercise-natural-language-processing", term: "natural language processing", definition: "A machine learning task that finds patterns within large data sets in order to recognize everyday language. This field of study drives better human-computer interaction and aids machines in better understanding human language.", zh: "自然語言處理", page: 1, source: "exercise" },
  { id: "p1-exercise-turing-test", term: "Turing Test", definition: "An assessment developed in 1950 to evaluate the ability of a machine to mimic human behaviour. It involves a human evaluator who undertakes natural language conversations with another human and a machine and rates the conversations.", zh: "圖靈測試", page: 1, source: "exercise" },
  { id: "p1-exercise-neural-networks", term: "neural networks", definition: "Learning models based on the human nervous system and brain. Based on the activity of neurons, they are used to solve tasks that would be too difficult for traditional methods of programming.", zh: "神經網路", page: 1, source: "exercise" },

  { id: "p2-exercise-threaten", term: "threaten", definition: "to be a danger to someone or something; likely to cause harm or damage", zh: "威脅", page: 2, source: "exercise" },
  { id: "p2-exercise-track-down", term: "track down", definition: "to find something or someone after looking for them in a lot of different places", zh: "追查找到", page: 2, source: "exercise" },
  { id: "p2-exercise-breakthrough", term: "breakthrough", definition: "a sudden solution of a problem leading to further advances, especially in science", zh: "重大進展；突破", page: 2, source: "exercise" },
  { id: "p2-exercise-optical-character-recognition", term: "Optical Character Recognition (OCR)", definition: "the branch of computer science that involves a computer reading printed or written text", zh: "光學字元辨識", page: 2, source: "exercise" },
  { id: "p2-exercise-hurdle", term: "hurdle", definition: "a problem or difficulty", zh: "障礙；困難", page: 2, source: "exercise" },
  { id: "p2-exercise-primed", term: "primed", definition: "ready to do a specific action", zh: "準備好；蓄勢待發", page: 2, source: "exercise" },
  { id: "p2-exercise-obsolete", term: "obsolete", definition: "technology that is no longer used because something new has been invented", zh: "過時的；淘汰的", page: 2, source: "exercise" },
  { id: "p2-exercise-warning-sign", term: "warning sign", definition: "an early signal that something bad or dangerous might happen", zh: "警告徵兆", page: 2, source: "exercise" },
  { id: "p2-exercise-virtual-assistant", term: "virtual assistant", definition: "a computer program or device connected to the internet that can understand spoken questions and instructions, designed to help you make plans, find answers to questions, etc.", zh: "虛擬助理", page: 2, source: "exercise" },
  { id: "p2-exercise-sentient", term: "sentient", definition: "able to perceive or feel things; conscious", zh: "有感知的；有意識的", page: 2, source: "exercise" },

  { id: "p2-article-self-sufficient", term: "self-sufficient", definition: "able to do or produce everything that you need without the help of other people", zh: "自給自足的；獨立自主的", page: 2, source: "article-highlight" },
  { id: "p2-article-backbone", term: "backbone", definition: "core", zh: "核心；支柱", page: 2, source: "article-highlight" },
  { id: "p2-article-constant-feedback-loop", term: "constant feedback loop", definition: "when outputs of a system are routed back as inputs as part of a chain of cause-and-effect", zh: "持續回饋迴路", page: 2, source: "article-highlight" },
  { id: "p2-article-overlooked", term: "overlooked", definition: "failed to see or notice something", zh: "被忽略；未被注意", page: 2, source: "article-highlight" },
  { id: "p2-article-unpredictable", term: "unpredictable", definition: "likely to change suddenly and without reason, so it cannot be foreseen or expected", zh: "無法預測的", page: 2, source: "article-highlight" },
  { id: "p2-article-nuances", term: "nuances", definition: "fine differences and gradations of meaning", zh: "細微差異；語意差別", page: 2, source: "article-highlight" },
  { id: "p2-article-indistinguishable", term: "indistinguishable", definition: "impossible to judge as being different when compared to another similar thing", zh: "無法區分的", page: 2, source: "article-highlight" },
  { id: "p2-article-uncover", term: "uncover", definition: "to discover something that was previously hidden or secret", zh: "發現；揭露", page: 2, source: "article-highlight" },
  { id: "p2-article-mountains-of-relevant-data", term: "mountains of relevant data", definition: "vast quantities of information relevant to the task", zh: "大量相關資料", page: 2, source: "article-highlight" },
  { id: "p2-article-contextual-meaning", term: "contextual meaning", definition: "understanding language and words from how they are used in practice", zh: "上下文意義；語境意義", page: 2, source: "article-highlight" },

  { id: "p2-exercise-self-sufficient", term: "self-sufficient", definition: "able to do or produce everything that you need without the help of other people", zh: "自給自足的；獨立自主的", page: 2, source: "exercise" },
  { id: "p2-exercise-backbone", term: "backbone", definition: "core", zh: "核心；支柱", page: 2, source: "exercise" },
  { id: "p2-exercise-constant-feedback-loop", term: "constant feedback loop", definition: "when outputs of a system are routed back as inputs as part of a chain of cause-and-effect", zh: "持續回饋迴路", page: 2, source: "exercise" },
  { id: "p2-exercise-overlook", term: "overlook", definition: "to fail to see or notice something", zh: "忽略；未注意", page: 2, source: "exercise" },
  { id: "p2-exercise-unpredictable", term: "unpredictable", definition: "likely to change suddenly and without reason, so it cannot be foreseen or expected", zh: "無法預測的", page: 2, source: "exercise" },
  { id: "p2-exercise-nuances", term: "nuances", definition: "fine differences and gradations of meaning", zh: "細微差異；語意差別", page: 2, source: "exercise" },
  { id: "p2-exercise-indistinguishable", term: "indistinguishable", definition: "impossible to judge as being different when compared to another similar thing", zh: "無法區分的", page: 2, source: "exercise" },
  { id: "p2-exercise-uncover", term: "uncover", definition: "to discover something that was previously hidden or secret", zh: "發現；揭露", page: 2, source: "exercise" },
  { id: "p2-exercise-mountains-of-data", term: "mountains of data", definition: "vast quantities of information", zh: "大量資料", page: 2, source: "exercise" },
  { id: "p2-exercise-contextual-meaning", term: "contextual meaning", definition: "understanding language and words from how they are used in practice", zh: "上下文意義；語境意義", page: 2, source: "exercise" },

  { id: "p3-exercise-mainstream", term: "mainstream", definition: "mass market", zh: "主流的", page: 3, source: "exercise" },
  { id: "p3-exercise-diagnostics", term: "diagnostics", definition: "identifying a particular illness or problem using a combination of signs and symptoms", zh: "診斷", page: 3, source: "exercise" },
  { id: "p3-exercise-predictive", term: "predictive", definition: "a computer system that forecasts what is wanted or needed is ________", zh: "預測的", page: 3, source: "exercise" },
  { id: "p3-exercise-maintenance", term: "maintenance", definition: "the work needed to keep a road, building, machine, etc. in good condition", zh: "維護；保養", page: 3, source: "exercise" },
  { id: "p3-exercise-advancement", term: "advancement", definition: "helping something to make progress or succeed", zh: "進步；發展", page: 3, source: "exercise" },
  { id: "p3-exercise-quantify", term: "quantify", definition: "measure, assess, evaluate", zh: "量化；以數字衡量", page: 3, source: "exercise" },
  { id: "p3-exercise-generative-adversarial-network", term: "Generative Adversarial Network (GAN)", definition: "a type of machine learning system, where two neural networks contest with each other in a zero-sum game framework", zh: "生成對抗網路", page: 3, source: "exercise" },
  { id: "p3-exercise-settle", term: "settle", definition: "to pay, especially money that is owed", zh: "償還；結清", page: 3, source: "exercise" },
  { id: "p3-exercise-counterfeit", term: "counterfeit", definition: "an object that is not genuine, but has been made to look like the original of something, usually for dishonest or illegal purposes", zh: "仿造的；偽造的", page: 3, source: "exercise" },
  { id: "p3-exercise-labelled-data", term: "labelled data", definition: "a group of samples with one specific meaning or tag", zh: "標記資料", page: 3, source: "exercise" },
  { id: "p3-exercise-claim", term: "claim", definition: "a written request to an organization to pay you money which you believe it owes you", zh: "索賠申請；要求付款", page: 3, source: "exercise" },
  { id: "p3-exercise-spectrum-sharing", term: "spectrum sharing", definition: "the simultaneous usage of a specific radio frequency band in a specific geographical area by a number of independent entities", zh: "頻譜共享", page: 3, source: "exercise" },
  { id: "p3-exercise-mugshot", term: "mugshot", definition: "a photograph taken by the police of a person who has been charged with a crime", zh: "警方拍攝的嫌犯照片", page: 3, source: "exercise" },
  { id: "p3-exercise-come-under-fire", term: "come under fire", definition: "to be criticized severely for something you have done", zh: "遭到嚴厲批評", page: 3, source: "exercise" },
  { id: "p3-exercise-landmark", term: "landmark", definition: "a distinctive easily recognizable feature, building or place which helps you navigate and know where you are", zh: "地標", page: 3, source: "exercise" },
  { id: "p3-exercise-lawsuit", term: "lawsuit", definition: "a claim or complaint against somebody that a person or an organization can make in court", zh: "訴訟；官司", page: 3, source: "exercise" },
  { id: "p3-exercise-biometric", term: "biometric", definition: "using measurements of human features, such as fingers or eyes, in order to identify people", zh: "生物辨識的", page: 3, source: "exercise" },
  { id: "p3-exercise-reading", term: "reading", definition: "a measurement; the amount or number shown on an instrument used for measuring something", zh: "讀數；測量值", page: 3, source: "exercise" },
  { id: "p3-exercise-surveillance", term: "surveillance", definition: "the careful watching of a person or place, especially by the police or army, because of a crime that has happened or is expected", zh: "監視；監控", page: 3, source: "exercise" },
  { id: "p3-exercise-address", term: "address", definition: "to give attention to or deal with a matter, problem or issue", zh: "處理；應對", page: 3, source: "exercise" },
  { id: "p3-exercise-stalker", term: "stalker", definition: "a person who illegally follows and watches someone over a period of time", zh: "跟蹤者", page: 3, source: "exercise" },
  { id: "p3-exercise-pore", term: "pore", definition: "one of the very small holes in your skin that sweat can pass through", zh: "毛孔；小孔", page: 3, source: "exercise" },
  { id: "p3-exercise-mitigate", term: "mitigate", definition: "to make something less harmful, serious, unpleasant, bad, etc.", zh: "減輕；緩和", page: 3, source: "exercise" },
  { id: "p3-exercise-determination", term: "determination", definition: "the act of finding out or calculating something", zh: "判定；測定；決心", page: 3, source: "exercise" },

  { id: "p4-exercise-donation", term: "donation", definition: "a gift of money or goods to a charity, fund or collection in order to help them", zh: "捐贈", page: 4, source: "exercise" },
  { id: "p4-exercise-tailor", term: "tailor", definition: "to make or adapt something for a particular purpose; to customize", zh: "量身修改；訂製", page: 4, source: "exercise" },
  { id: "p4-exercise-retailer", term: "retailer", definition: "a person or business that sells goods to the public", zh: "零售商", page: 4, source: "exercise" },
  { id: "p4-exercise-suspicious", term: "suspicious", definition: "feeling that somebody has done something wrong, illegal or dishonest, without having any proof", zh: "懷疑的；可疑的", page: 4, source: "exercise" },
  { id: "p4-exercise-brainy", term: "brainy", definition: "very intelligent", zh: "聰明的", page: 4, source: "exercise" },
  { id: "p4-exercise-accustomed", term: "accustomed", definition: "familiar with something and accepting it as normal or usual", zh: "習慣的；適應的", page: 4, source: "exercise" },
  { id: "p4-exercise-shoplifter", term: "shoplifter", definition: "a person who steals goods from a store by deliberately leaving without paying for them", zh: "商店扒手；竊賊", page: 4, source: "exercise" },
  { id: "p4-exercise-impostor", term: "impostor", definition: "a person who pretends to be somebody else in order to trick people", zh: "冒充者；騙子", page: 4, source: "exercise" },
  { id: "p4-exercise-middleman", term: "middleman", definition: "someone who communicates or makes business transactions between two people or groups", zh: "中間人；仲介", page: 4, source: "exercise" },
  { id: "p4-exercise-ledger", term: "ledger", definition: "a book in which things are regularly recorded, especially business activities and money received or paid", zh: "帳本；分類帳", page: 4, source: "exercise" },
  { id: "p4-exercise-decentralized", term: "decentralized", definition: "organizations or activities which are not controlled from one central place, but happen in many different places", zh: "去中心化的", page: 4, source: "exercise" },
  { id: "p4-exercise-timestamp", term: "timestamp", definition: "a record in printed or digital form that shows the time at which something happened or was done", zh: "時間戳記", page: 4, source: "exercise" },
  { id: "p4-exercise-enforce", term: "enforce", definition: "to make people obey a law, or to make a particular situation happen or be accepted", zh: "執行；強制實施", page: 4, source: "exercise" },
  { id: "p4-exercise-asset", term: "asset", definition: "something having value, such as a possession or property, that is owned by a person, business, or organization", zh: "資產", page: 4, source: "exercise" },
  { id: "p4-exercise-underpin", term: "underpin", definition: "to give support, strength, or a basic structure to something, e.g. an argument, process or strategy", zh: "支撐；奠定基礎", page: 4, source: "exercise" },
  { id: "p4-exercise-consensus", term: "consensus", definition: "a generally accepted opinion or decision among a group of people", zh: "共識", page: 4, source: "exercise" },
  { id: "p4-exercise-tamper", term: "tamper", definition: "to make changes to something without permission or illegally, often with the intention of damaging it, or altering it for criminal purposes", zh: "篡改", page: 4, source: "exercise" },
  { id: "p4-exercise-peer-to-peer", term: "peer-to-peer", definition: "involving sharing files or other resources between computers connected through a network, rather than using a central server", zh: "點對點的", page: 4, source: "exercise" },

  { id: "p5-exercise-intermediary", term: "intermediary", definition: "a person or organization that makes business or financial arrangements between companies or organizations that do not deal with each other directly", zh: "中間人；仲介機構", page: 5, source: "exercise" },
  { id: "p5-exercise-pending", term: "pending", definition: "about to happen or waiting to happen", zh: "即將發生的；待處理的", page: 5, source: "exercise" },
  { id: "p5-exercise-node", term: "node", definition: "a place where things such as lines or systems join", zh: "節點", page: 5, source: "exercise" },
  { id: "p5-exercise-falsify", term: "falsify", definition: "to change a written record or information so that it is no longer true, usually with criminal intentions", zh: "偽造；竄改", page: 5, source: "exercise" },
  { id: "p5-exercise-hash", term: "hash", definition: "a function that converts an input of letters and numbers into an encrypted output of a fixed length", zh: "雜湊值；雜湊函數", page: 5, source: "exercise" },
  { id: "p5-exercise-fork", term: "fork", definition: "what happens when a blockchain diverges into two potential paths forward", zh: "分叉；分支", page: 5, source: "exercise" },
  { id: "p5-exercise-immutability", term: "immutability", definition: "the ability of a blockchain ledger to remain unchanged, and for a blockchain to remain unaltered", zh: "不可變性；不可竄改性", page: 5, source: "exercise" },
  { id: "p5-exercise-funds", term: "funds", definition: "money, often for a specific purpose such as a project or business venture", zh: "資金；款項", page: 5, source: "exercise" },
  { id: "p5-exercise-downtime", term: "downtime", definition: "the time during which a machine, especially a computer, is not working or is not able to be used", zh: "停機時間", page: 5, source: "exercise" },
  { id: "p5-exercise-future-proof", term: "future-proof", definition: "to design software, a computer, etc. so that it can still be used for many years, even when technology changes", zh: "能適應未來的；面向未來的", page: 5, source: "exercise" }
];

const normalizeAnswer = (value) =>
  String(value).trim().toLocaleLowerCase("en").replace(/\s+/g, " ");

const gradeSpelling = (input, term) =>
  normalizeAnswer(input) === normalizeAnswer(term);

const shuffled = (values, random = Math.random) => {
  const copy = [...values];
  for (let index = copy.length - 1; index > 0; index -= 1) {
    const target = Math.floor(random() * (index + 1));
    [copy[index], copy[target]] = [copy[target], copy[index]];
  }
  return copy;
};

const selectQuizEntries = (pool, random = Math.random) =>
  shuffled(pool, random).slice(0, 10);

function createChoiceQuestion(entry, pool, mode, random = Math.random) {
  const toChinese = mode === "term-to-zh";
  const prompt = toChinese ? entry.term : entry.definition;
  const answer = toChinese ? entry.zh : entry.term;
  const distractors = pool
    .filter((item) => item.id !== entry.id)
    .map((item) => toChinese ? item.zh : item.term)
    .filter((value, index, values) => value !== answer && values.indexOf(value) === index);
  return {
    entryId: entry.id,
    prompt,
    answer,
    options: shuffled([answer, ...shuffled(distractors, random).slice(0, 3)], random)
  };
}

const defaultProgress = () =>
  ({ familiar: [], review: [], mistakes: [], lastScore: null });

function mergeProgress(value, validIds) {
  const safe = value && typeof value === "object" ? value : {};
  const ids = (list) => [
    ...new Set(Array.isArray(list) ? list.filter((id) => validIds.has(id)) : [])
  ];
  const score = safe.lastScore
    && Number.isInteger(safe.lastScore.correct)
    && Number.isInteger(safe.lastScore.total)
    ? { correct: safe.lastScore.correct, total: safe.lastScore.total }
    : null;
  return {
    familiar: ids(safe.familiar),
    review: ids(safe.review),
    mistakes: ids(safe.mistakes),
    lastScore: score
  };
}

function progressStorage(storage, key) {
  return {
    load(validIds) {
      try {
        return mergeProgress(JSON.parse(storage.getItem(key) || "null"), validIds);
      } catch {
        return defaultProgress();
      }
    },
    save(progress) {
      try {
        storage.setItem(key, JSON.stringify(progress));
        return true;
      } catch {
        return false;
      }
    },
    clear() {
      try {
        storage.removeItem(key);
        return true;
      } catch {
        return false;
      }
    }
  };
}


const STORAGE_KEY = "cs-vocabulary-progress-v1";
const QUIZ_MODES = new Set(["term-to-zh", "definition-to-term", "spelling"]);
const byId = new Map(VOCABULARY.map((entry) => [entry.id, entry]));
const validIds = new Set(byId.keys());

const elements = {
  viewButtons: [...document.querySelectorAll("[data-view-button]")],
  views: [...document.querySelectorAll(".view")],
  status: document.querySelector("#status-message"),
  cardOrder: document.querySelector("#card-order"),
  cardCount: document.querySelector("#card-count"),
  cardFront: document.querySelector("#card-front"),
  cardBack: document.querySelector("#card-back"),
  cardTerm: document.querySelector("#card-term"),
  cardDefinition: document.querySelector("#card-definition"),
  cardZh: document.querySelector("#card-zh"),
  cardSource: document.querySelector("#card-source"),
  cardPage: document.querySelector("#card-page"),
  previousCard: document.querySelector("#previous-card"),
  flipCard: document.querySelector("#flip-card"),
  nextCard: document.querySelector("#next-card"),
  markReview: document.querySelector("#mark-review"),
  markFamiliar: document.querySelector("#mark-familiar"),
  quizMode: document.querySelector("#quiz-mode"),
  quizScope: document.querySelector("#quiz-scope"),
  startQuiz: document.querySelector("#start-quiz"),
  quizStage: document.querySelector("#quiz-stage"),
  quizProgress: document.querySelector("#quiz-progress"),
  quizPrompt: document.querySelector("#quiz-prompt"),
  quizOptions: document.querySelector("#quiz-options"),
  spellingForm: document.querySelector("#spelling-form"),
  spellingAnswer: document.querySelector("#spelling-answer"),
  submitSpelling: document.querySelector("#submit-spelling"),
  quizFeedback: document.querySelector("#quiz-feedback"),
  correctAnswer: document.querySelector("#quiz-correct-answer"),
  nextQuestion: document.querySelector("#next-question"),
  lastScore: document.querySelector("#last-score"),
  mistakeList: document.querySelector("#mistake-list"),
  retryMistakes: document.querySelector("#retry-mistakes"),
  clearProgress: document.querySelector("#clear-progress")
};

const unavailableStorage = {
  getItem() { throw new Error("Storage unavailable"); },
  setItem() { throw new Error("Storage unavailable"); },
  removeItem() { throw new Error("Storage unavailable"); }
};

let storageAvailable = true;
let store;
try {
  const storage = window.localStorage;
  const probeKey = `${STORAGE_KEY}-probe`;
  storage.setItem(probeKey, "1");
  storage.removeItem(probeKey);
  store = progressStorage(storage, STORAGE_KEY);
} catch {
  storageAvailable = false;
  store = progressStorage(unavailableStorage, STORAGE_KEY);
}

const state = {
  cardOrder: [...VOCABULARY],
  cardIndex: 0,
  flipped: false,
  progress: store.load(validIds),
  quiz: null
};

function shuffle(values) {
  const result = [...values];
  for (let index = result.length - 1; index > 0; index -= 1) {
    const target = Math.floor(Math.random() * (index + 1));
    [result[index], result[target]] = [result[target], result[index]];
  }
  return result;
}

function announce(message = "") {
  const warning = storageAvailable
    ? ""
    : "Storage is unavailable. Progress will continue for this session only.";
  elements.status.textContent = [warning, message].filter(Boolean).join(" ");
}

function saveProgress(message = "") {
  if (!store.save(state.progress)) {
    storageAvailable = false;
  }
  announce(message);
}

function setView(viewId) {
  for (const view of elements.views) {
    view.hidden = view.id !== viewId;
  }
  for (const button of elements.viewButtons) {
    button.setAttribute("aria-pressed", String(button.dataset.viewButton === viewId));
  }
  if (viewId === "mistakes-view") renderMistakes();
}

function renderCard() {
  const entry = state.cardOrder[state.cardIndex];
  elements.cardCount.textContent = `${state.cardIndex + 1} / ${state.cardOrder.length}`;
  elements.cardTerm.textContent = entry.term;
  elements.cardDefinition.textContent = entry.definition;
  elements.cardZh.textContent = entry.zh;
  elements.cardSource.textContent = `Source: ${entry.source}`;
  elements.cardPage.textContent = `PDF page ${entry.page}`;
  elements.cardFront.hidden = state.flipped;
  elements.cardBack.hidden = !state.flipped;
  elements.flipCard.textContent = state.flipped ? "Show term" : "Flip";

  const familiar = state.progress.familiar.includes(entry.id);
  const review = state.progress.review.includes(entry.id);
  elements.markFamiliar.textContent = familiar ? "✓ Familiar" : "Mark familiar";
  elements.markFamiliar.setAttribute("aria-pressed", String(familiar));
  elements.markReview.textContent = review ? "★ Review marked" : "Mark for review";
  elements.markReview.setAttribute("aria-pressed", String(review));
}

function moveCard(offset) {
  state.cardIndex = (state.cardIndex + offset + state.cardOrder.length) % state.cardOrder.length;
  state.flipped = false;
  renderCard();
}

function markCard(kind) {
  const entryId = state.cardOrder[state.cardIndex].id;
  const other = kind === "familiar" ? "review" : "familiar";
  const selected = new Set(state.progress[kind]);
  selected.has(entryId) ? selected.delete(entryId) : selected.add(entryId);
  state.progress[kind] = [...selected];
  state.progress[other] = state.progress[other].filter((id) => id !== entryId);
  saveProgress(kind === "familiar" ? "Familiar status updated." : "Review status updated.");
  renderCard();
}

function renderMistakes() {
  elements.mistakeList.replaceChildren();
  const mistakes = state.progress.mistakes.map((id) => byId.get(id)).filter(Boolean);
  if (mistakes.length === 0) {
    const item = document.createElement("li");
    item.textContent = "No mistakes saved yet.";
    elements.mistakeList.append(item);
  } else {
    for (const entry of mistakes) {
      const item = document.createElement("li");
      item.textContent = `${entry.term} — ${entry.zh} (PDF page ${entry.page}, ${entry.source})`;
      elements.mistakeList.append(item);
    }
  }
  const score = state.progress.lastScore;
  elements.lastScore.textContent = score
    ? `Last score: ${score.correct} / ${score.total}`
    : "No completed quiz yet.";
  elements.retryMistakes.disabled = mistakes.length === 0;
}

function resetQuizFeedback() {
  elements.quizFeedback.textContent = "";
  elements.quizFeedback.removeAttribute("data-result");
  elements.correctAnswer.textContent = "";
  elements.nextQuestion.hidden = true;
}

function renderQuizQuestion() {
  const quiz = state.quiz;
  const entry = quiz.entries[quiz.index];
  quiz.locked = false;
  resetQuizFeedback();
  elements.quizProgress.textContent = `Question ${quiz.index + 1} / ${quiz.entries.length}`;
  elements.quizOptions.replaceChildren();
  elements.spellingAnswer.value = "";

  if (quiz.mode === "spelling") {
    quiz.question = { entryId: entry.id, prompt: entry.definition, answer: entry.term };
    elements.quizPrompt.textContent = quiz.question.prompt;
    elements.quizOptions.hidden = true;
    elements.spellingForm.hidden = false;
    elements.spellingAnswer.disabled = false;
    elements.submitSpelling.disabled = false;
    elements.spellingAnswer.focus();
    return;
  }

  quiz.question = createChoiceQuestion(entry, VOCABULARY, quiz.mode);
  elements.quizPrompt.textContent = quiz.question.prompt;
  elements.quizOptions.hidden = false;
  elements.spellingForm.hidden = true;
  for (const option of quiz.question.options) {
    const button = document.createElement("button");
    button.type = "button";
    button.textContent = option;
    button.addEventListener("click", () => answerQuestion(option));
    elements.quizOptions.append(button);
  }
}

function answerQuestion(answer) {
  const quiz = state.quiz;
  if (!quiz || quiz.locked) return;
  quiz.locked = true;
  const entryId = quiz.question.entryId;
  const correct = quiz.mode === "spelling"
    ? gradeSpelling(answer, quiz.question.answer)
    : answer === quiz.question.answer;

  for (const button of elements.quizOptions.querySelectorAll("button")) {
    button.disabled = true;
  }
  elements.spellingAnswer.disabled = true;
  elements.submitSpelling.disabled = true;
  elements.quizFeedback.dataset.result = correct ? "correct" : "incorrect";
  elements.quizFeedback.textContent = correct ? "✓ Correct" : "✗ Incorrect";
  elements.correctAnswer.textContent = `Correct answer: ${quiz.question.answer}`;
  elements.nextQuestion.hidden = false;

  const mistakes = new Set(state.progress.mistakes);
  if (correct) {
    quiz.correct += 1;
    if (quiz.mistakesOnly) mistakes.delete(entryId);
  } else {
    mistakes.add(entryId);
  }
  state.progress.mistakes = [...mistakes];
  saveProgress();
  elements.nextQuestion.focus();
}

function finishQuiz() {
  const quiz = state.quiz;
  state.progress.lastScore = { correct: quiz.correct, total: quiz.entries.length };
  saveProgress(`Quiz complete: ${quiz.correct} / ${quiz.entries.length}.`);
  elements.quizProgress.textContent = "Quiz complete";
  elements.quizPrompt.textContent = `Score: ${quiz.correct} / ${quiz.entries.length}`;
  elements.quizOptions.replaceChildren();
  elements.quizOptions.hidden = false;
  elements.spellingForm.hidden = true;
  resetQuizFeedback();
  renderMistakes();
  state.quiz = null;
}

function nextQuestion() {
  if (!state.quiz || !state.quiz.locked) return;
  state.quiz.index += 1;
  if (state.quiz.index >= state.quiz.entries.length) {
    finishQuiz();
  } else {
    renderQuizQuestion();
  }
}

function startQuiz({ mistakesOnly = elements.quizScope.value === "mistakes" } = {}) {
  const pool = mistakesOnly
    ? state.progress.mistakes.map((id) => byId.get(id)).filter(Boolean)
    : VOCABULARY;
  if (pool.length === 0) {
    elements.quizStage.hidden = true;
    announce("There are no saved mistakes to retry.");
    return;
  }
  const entries = selectQuizEntries(pool);
  const selectedMode = elements.quizMode.value;
  state.quiz = {
    mode: QUIZ_MODES.has(selectedMode) ? selectedMode : "term-to-zh",
    mistakesOnly,
    entries,
    index: 0,
    correct: 0,
    locked: false,
    question: null
  };
  elements.quizStage.hidden = false;
  announce();
  renderQuizQuestion();
}

for (const button of elements.viewButtons) {
  button.addEventListener("click", () => setView(button.dataset.viewButton));
}

elements.cardOrder.addEventListener("change", () => {
  state.cardOrder = elements.cardOrder.value === "random"
    ? shuffle(VOCABULARY)
    : [...VOCABULARY];
  state.cardIndex = 0;
  state.flipped = false;
  renderCard();
});
elements.previousCard.addEventListener("click", () => moveCard(-1));
elements.nextCard.addEventListener("click", () => moveCard(1));
elements.flipCard.addEventListener("click", () => {
  state.flipped = !state.flipped;
  renderCard();
});
elements.markReview.addEventListener("click", () => markCard("review"));
elements.markFamiliar.addEventListener("click", () => markCard("familiar"));
elements.startQuiz.addEventListener("click", () => startQuiz());
elements.nextQuestion.addEventListener("click", nextQuestion);
elements.spellingForm.addEventListener("submit", (event) => {
  event.preventDefault();
  answerQuestion(elements.spellingAnswer.value);
});
elements.retryMistakes.addEventListener("click", () => {
  elements.quizScope.value = "mistakes";
  setView("quiz-view");
  startQuiz({ mistakesOnly: true });
});
elements.clearProgress.addEventListener("click", () => {
  if (!window.confirm("確定要清除所有熟悉度、錯題和測驗紀錄嗎？")) return;
  state.progress = defaultProgress();
  if (!store.clear()) storageAvailable = false;
  renderCard();
  renderMistakes();
  announce("Saved progress cleared.");
});

renderCard();
renderMistakes();
announce();

})();
